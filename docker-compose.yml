services:
  backend:
    build: .
    environment:
      DATABASE_URL: $${DATABASE_URL-sqlserver://69.30.247.60:1433;database=TrainHub360;user=sa;password=REPLACE_ME;trustServerCertificate=true}
      JWT_SECRET: $${JWT_SECRET-dev-secret}
      PORT: 4000
    ports: ["4000:4000"]
  frontend:
    build:
      context: ./frontend
    ports: ["5173:5173"]

