services:
  backend:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: $${DATABASE_URL}
      JWT_SECRET: $${JWT_SECRET}
      PORT: 4000
    ports: ["4000:4000"]
  frontend:
    build:
      context: ./frontend
    ports: ["5173:5173"]

